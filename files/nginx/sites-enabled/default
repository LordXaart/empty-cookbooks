server {
	listen 80 default_server;
	listen 443 default_server;

	root /var/www/html;

	# Add index.php to the list if you are using PHP
	index index.html;

	location ~ /.well-known {
          allow all;
     }
}